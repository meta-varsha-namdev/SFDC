trigger BillToContactOpportunity on Opportunity (before insert) 
{
	for(Opportunity opp : trigger.new)
    {	
        if(opp.BillToContact__c != null && opp.Manager__c == null)
        opp.Manager__c = opp.BillToContact__r.AccountId;               
    }
}