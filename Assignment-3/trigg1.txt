trigger TeacherNotHindi on Contact (before insert, before update)
{
	for(Contact con : trigger.new)
    {
        if(con.Subjects__c == 'Hindi')
            con.addError('Hindi Teacher already exists');
    }
}